# TinyFusion Progress Log
Started: 2026-06-16
---

## Codebase Patterns
- TinyFusion is a Rust workspace with two crates: tinyfusion-core and tinyfusion-gui
- Core uses Axum for HTTP server, Tokio for async runtime
- GUI uses Tauri v2 with React frontend (to be added)
- Design system follows refined developer console aesthetic (dark-first, Inter font, Lucide icons)
- `tracing_subscriber::fmt().init()` must be called in main.rs for `info!`/`tracing` logs to appear on stdout
- Use `tower::ServiceExt` with `axum::body::to_bytes` for unit testing routes without starting a real server

## 2026-06-16 - US-001
- What was implemented: Rust workspace scaffolding with two crates (tinyfusion-core and tinyfusion-gui)
- Files changed:
  - Cargo.toml (workspace root)
  - tinyfusion-core/Cargo.toml (core crate with axum, tokio, reqwest, serde dependencies)
  - tinyfusion-core/src/main.rs (entry point)
  - tinyfusion-core/src/lib.rs (module declarations)
  - tinyfusion-core/src/server.rs (Axum server with /health endpoint and graceful shutdown)
  - tinyfusion-core/src/sniffer.rs (request state detection with tests)
  - tinyfusion-core/src/session.rs (session manager with in-memory storage and tests)
  - tinyfusion-core/src/moa/mod.rs (MoA worker/judge module with tests)
  - tinyfusion-core/src/keepalive.rs (SSE keepalive module)
  - tinyfusion-core/src/oracle.rs (verification command execution with tests)
  - tinyfusion-core/src/proxy.rs (upstream request forwarding)
  - tinyfusion-gui/Cargo.toml (Tauri v2 crate)
  - tinyfusion-gui/build.rs (Tauri build script)
  - tinyfusion-gui/src/main.rs (Tauri app entry point)
  - tinyfusion-gui/tauri.conf.json (Tauri config: 1200x800 window, "TinyFusion" title)
  - tinyfusion-gui/icons/*.png (placeholder RGBA icons)
  - dist/index.html (minimal frontend placeholder)
- All 20 tests pass, cargo check passes for both crates
- **Learnings for future iterations:**
  - Tauri v2 requires RGBA (not RGB) PNG icons, otherwise build fails
  - Tauri v2 capabilities directory must not exist if using auto-generated defaults
  - `cargo check` is the typecheck equivalent for Rust — must pass for all stories
---

## 2026-06-16 - US-002
- What was implemented: Axum HTTP server setup with health endpoint tests, tracing initialization, and 404 test
- Files changed:
  - tinyfusion-core/src/server.rs (extracted `app()` builder for testability, added health check + 404 tests)
  - tinyfusion-core/src/main.rs (added `tracing_subscriber::fmt().init()` for startup logs)
  - tinyfusion-core/Cargo.toml (added `tower` dependency for route testing)
- 22 tests pass (20 existing + 2 new server tests), cargo check passes
- **Learnings for future iterations:**
  - `tower::ServiceExt` allows testing axum routes without spawning a real server
  - Server `app()` should be a separate public function so tests can construct it independently
---

## 2026-06-16 - US-003
- What was implemented: Config file loading from ~/.tinyfusion/config.json with default creation, typed parsing via serde, and model count logging
- Files changed:
  - tinyfusion-core/src/config.rs (new: Config, ModelConfig, WorkspaceConfig structs, load/default creation, 6 tests)
  - tinyfusion-core/src/lib.rs (added `pub mod config`)
  - tinyfusion-core/src/main.rs (load config at startup, pass port to server::run)
  - tinyfusion-core/src/server.rs (run() now accepts port parameter)
  - tinyfusion-core/Cargo.toml (added tempfile dev dependency for tests)
- 28 tests pass (22 existing + 6 new config tests), cargo check passes
- **Learnings for future iterations:**
  - `tempfile::NamedTempFile` deletes the file when dropped — use `std::fs::write` to a temp directory instead for tests that need the file to persist during the test
  - Config loading uses `HOME` env var with `USERPROFILE` fallback for Windows compatibility
  - Server `run()` function accepts `port: u16` rather than hardcoding — this pattern should continue for other configurable values
---

## 2026-06-16 13:07 - US-004
- What was implemented: Verified Tauri App Shell acceptance criteria — all scaffolded components from US-001 satisfy US-004 requirements out of the box
- Files changed:
  - prd.json (set passes: true for US-004)
  - progress.txt (appended this entry)
- Verified:
  - `cargo check --package tinyfusion-gui` passes
  - `cargo tauri build` succeeds, producing TinyFusion.app and TinyFusion_0.1.0_aarch64.dmg
  - App bundle contains correct Info.plist, MacOS binary, and Resources
  - Icons are RGBA PNG (verified with `file` command)
  - tauri.conf.json: 1200x800 window, "TinyFusion" title, frontendDist points to ../dist
  - dist/index.html exists and is minimal but valid
- **Learnings for future iterations:**
  - `cargo tauri build` is the correct verification for Tauri app shell (produces .app + .dmg on macOS)
  - App bundle structure: Contents/{Info.plist, MacOS/, Resources/}
---
